{
  "private": true,
  "name": "turborepo-react-nest-test",
  "packageManager": "npm@10.8.1",
  "devDependencies": {
    "turbo": "v2.1.3"
  },
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**"
      ]
    }
  },
  "workspaces": [
    "apps/*"
  ]
}
